import React, { use } from 'react';

const Users2 = ({users2}) => {
    const users = use(users2);

    console.log('users2', users);
    return (
        <div>
            
        </div>
    );
};

export default Users2;